cmake_minimum_required(VERSION 3.15)
project(langnes_serialization_json)

add_library(langnes_serialization_json INTERFACE)
target_include_directories(langnes_serialization_json INTERFACE include)
target_compile_features(langnes_serialization_json INTERFACE cxx_std_17)

add_subdirectory(example)
